# Superseded — the beauty pipeline lives in `lab/sketch/`

This directory is the FIRST beauty pipeline (single-card generation scored by
an on-device vision critic; it plateaued at 6/10 on the generator's ceiling
and is kept for the record).

The shipping pipeline — purchased design kit → theme pack → LLM-authored L0
cards → three rendered rails → fill gate + calibrated judge — is
`lab/sketch/run_kit.py`. Start with `lab/sketch/README.md`.
